version: '3.9'
services:
  backend:
    build: ./backend
    env_file:
      - backend/.env
    ports:
      - '8080:8080'
    volumes:
      - ./backend:/app
  frontend:
    build: ./frontend
    env_file:
      - frontend/.env
    ports:
      - '5173:4173'
    depends_on:
      - backend
