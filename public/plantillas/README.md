# Plantillas de Documentos

Coloca en esta carpeta las plantillas de Excel (`.xlsx`) que Document AI utilizará para mapear los campos extraídos. El backend solo permitirá acceder a archivos dentro de este directorio para evitar rutas no seguras.
